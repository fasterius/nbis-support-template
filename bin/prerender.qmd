---
format:
    markdown:
        echo: false
        warning: false
        standalone: false
params:
    artifact_dir: "artifacts"
    cpus: 1
    meta: null
execute:
    fig-format: retina
---

# Pre-render

Text from the pre-render

```{r}
warning("This is a warning")
plot(1, 1)
data(iris)
write.table(head(iris), file.path(params$artifact_dir, "iris-head.txt"))
```
